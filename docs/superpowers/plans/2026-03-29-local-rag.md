# Local RAG System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sistema RAG 100% local com LightRAG + Ollama, pipeline de ingestão para Obsidian e Zotero, MCP server para Claude Code.

**Architecture:** LightRAG (HKUDS) expõe uma API REST local na porta 9621. Um pipeline Python lê documentos de múltiplas fontes, extrai texto, divide em chunks e envia para o LightRAG indexar. Um MCP server em Python faz a ponte entre Claude Code e a API do LightRAG.

**Tech Stack:** Python 3.11+, lightrag-hku, Ollama (qwen2.5:14b + nomic-embed-text), pymupdf, python-docx, openpyxl, bibtexparser, pyyaml, requests, mcp (Anthropic SDK)

---

## Estrutura de Arquivos

```
C:/workspace/pessoal/local-rag/
├── config.yaml              ← fontes, modelos, caminhos
├── ingest.py                ← entry point do pipeline
├── mcp_server.py            ← MCP server para Claude Code
├── watcher.py               ← daemon para novos arquivos
├── requirements.txt
├── .gitignore
├── src/
│   ├── __init__.py
│   ├── config_loader.py     ← carrega e valida config.yaml
│   ├── extractor.py         ← extração de texto por formato
│   ├── chunker.py           ← chunking com metadados
│   ├── checkpoint.py        ← rastreia arquivos processados
│   └── lightrag_client.py   ← cliente HTTP para API do LightRAG
└── tests/
    ├── __init__.py
    ├── fixtures/
    │   └── sample.md
    ├── test_extractor.py
    ├── test_chunker.py
    ├── test_checkpoint.py
    └── test_lightrag_client.py
```

---

## Task 1: Ambiente e dependências

**Files:**
- Create: `C:/workspace/pessoal/local-rag/requirements.txt`
- Create: `C:/workspace/pessoal/local-rag/.gitignore`
- Create: `C:/workspace/pessoal/local-rag/config.yaml`
- Create: `C:/workspace/pessoal/local-rag/src/__init__.py`
- Create: `C:/workspace/pessoal/local-rag/tests/__init__.py`
- Create: `C:/workspace/pessoal/local-rag/tests/fixtures/sample.md`

- [ ] **Step 1: Configurar variável de ambiente OLLAMA_MODELS no Windows**

Abrir PowerShell como administrador e executar:
```powershell
[System.Environment]::SetEnvironmentVariable("OLLAMA_MODELS", "D:\ollama_models", "Machine")
```
Reiniciar o terminal após executar. Verificar:
```powershell
echo $env:OLLAMA_MODELS
# Expected: D:\ollama_models
```

- [ ] **Step 2: Instalar Ollama e baixar modelos**

Baixar instalador em https://ollama.com e instalar.
Após instalar, abrir novo terminal e executar:
```bash
ollama pull qwen2.5:14b
ollama pull nomic-embed-text
```
Verificar:
```bash
ollama list
# Expected: qwen2.5:14b e nomic-embed-text listados
```

- [ ] **Step 3: Instalar LightRAG**

```bash
cd C:/workspace/pessoal/local-rag
pip install "lightrag-hku[api]"
```

- [ ] **Step 4: Criar requirements.txt**

```
lightrag-hku[api]
pymupdf
python-docx
openpyxl
bibtexparser
pyyaml
requests
mcp
pytest
pytest-asyncio
```

- [ ] **Step 5: Criar .gitignore**

```
data/
__pycache__/
*.pyc
.env
*.log
checkpoint.json
```

- [ ] **Step 6: Criar config.yaml**

```yaml
sources:
  obsidian:
    path: "D:/Obsidian/vault/"
    include_all: true
    extensions: [".md"]

  zotero:
    path: "D:/_bib_Zotero/"
    include_all: true
    extensions: [".pdf", ".bib"]

lightrag:
  host: "localhost"
  port: 9621
  working_dir: "C:/workspace/pessoal/local-rag/data/lightrag_storage"

ollama:
  llm_model: "qwen2.5:14b"
  embedding_model: "nomic-embed-text"

chunking:
  chunk_size: 2000
  overlap: 200

checkpoint_file: "C:/workspace/pessoal/local-rag/data/checkpoint.json"
```

- [ ] **Step 7: Criar src/__init__.py e tests/__init__.py**

Ambos os arquivos ficam vazios:
```python
```

- [ ] **Step 8: Criar fixture de teste**

`tests/fixtures/sample.md`:
```markdown
# Performatividade Criptoeconômica

A performatividade criptoeconômica descreve como dispositivos técnicos como Bitcoin e Ethereum
constroem realidades econômicas ao serem adotados por atores sociais.

## Conceitos centrais

- **Dispositivo**: artefato técnico com capacidade de agência
- **Performatividade**: capacidade de constituir o que descreve
- **Blockchain**: substrato tecnológico distribuído
```

- [ ] **Step 9: Instalar dependências**

```bash
cd C:/workspace/pessoal/local-rag
pip install -r requirements.txt
```
Expected: todos os pacotes instalados sem erros.

- [ ] **Step 10: Commit inicial**

```bash
cd C:/workspace/pessoal/local-rag
git init
git add requirements.txt .gitignore config.yaml src/__init__.py tests/__init__.py tests/fixtures/sample.md
git commit -m "chore: project scaffold and dependencies"
```

---

## Task 2: Config loader

**Files:**
- Create: `C:/workspace/pessoal/local-rag/src/config_loader.py`

- [ ] **Step 1: Escrever o teste**

`tests/test_config_loader.py`:
```python
import pytest
import tempfile
import os
import yaml
from src.config_loader import load_config, ConfigError

VALID_CONFIG = {
    "sources": {
        "obsidian": {"path": "/tmp/vault", "include_all": True, "extensions": [".md"]},
        "zotero": {"path": "/tmp/zotero", "include_all": True, "extensions": [".pdf", ".bib"]}
    },
    "lightrag": {"host": "localhost", "port": 9621, "working_dir": "/tmp/storage"},
    "ollama": {"llm_model": "qwen2.5:14b", "embedding_model": "nomic-embed-text"},
    "chunking": {"chunk_size": 2000, "overlap": 200},
    "checkpoint_file": "/tmp/checkpoint.json"
}

def write_config(tmp_path, data):
    path = tmp_path / "config.yaml"
    path.write_text(yaml.dump(data))
    return str(path)

def test_load_valid_config(tmp_path):
    path = write_config(tmp_path, VALID_CONFIG)
    config = load_config(path)
    assert config["lightrag"]["port"] == 9621
    assert config["ollama"]["llm_model"] == "qwen2.5:14b"
    assert ".md" in config["sources"]["obsidian"]["extensions"]

def test_missing_lightrag_raises(tmp_path):
    data = {k: v for k, v in VALID_CONFIG.items() if k != "lightrag"}
    path = write_config(tmp_path, data)
    with pytest.raises(ConfigError, match="lightrag"):
        load_config(path)

def test_missing_file_raises():
    with pytest.raises(ConfigError, match="not found"):
        load_config("/nonexistent/config.yaml")
```

- [ ] **Step 2: Rodar o teste para ver falhar**

```bash
cd C:/workspace/pessoal/local-rag
pytest tests/test_config_loader.py -v
```
Expected: `ImportError` ou `ModuleNotFoundError` — arquivo ainda não existe.

- [ ] **Step 3: Implementar config_loader.py**

`src/config_loader.py`:
```python
import yaml
import os

class ConfigError(Exception):
    pass

def load_config(path: str) -> dict:
    if not os.path.exists(path):
        raise ConfigError(f"Config file not found: {path}")
    with open(path, "r", encoding="utf-8") as f:
        config = yaml.safe_load(f)
    for required in ["sources", "lightrag", "ollama", "chunking", "checkpoint_file"]:
        if required not in config:
            raise ConfigError(f"Missing required config section: '{required}'")
    return config
```

- [ ] **Step 4: Rodar o teste para ver passar**

```bash
pytest tests/test_config_loader.py -v
```
Expected: 3 testes PASSED.

- [ ] **Step 5: Commit**

```bash
git add src/config_loader.py tests/test_config_loader.py
git commit -m "feat: config loader with validation"
```

---

## Task 3: Extrator de texto

**Files:**
- Create: `C:/workspace/pessoal/local-rag/src/extractor.py`
- Create: `C:/workspace/pessoal/local-rag/tests/test_extractor.py`

- [ ] **Step 1: Escrever os testes**

`tests/test_extractor.py`:
```python
import pytest
import os
from src.extractor import extract_text, UnsupportedFormatError

FIXTURES = os.path.join(os.path.dirname(__file__), "fixtures")

def test_extract_markdown():
    path = os.path.join(FIXTURES, "sample.md")
    text = extract_text(path)
    assert "performatividade" in text.lower()
    assert "blockchain" in text.lower()

def test_unsupported_format(tmp_path):
    f = tmp_path / "file.xyz"
    f.write_text("conteudo")
    with pytest.raises(UnsupportedFormatError):
        extract_text(str(f))

def test_nonexistent_file():
    with pytest.raises(FileNotFoundError):
        extract_text("/nonexistent/file.md")
```

- [ ] **Step 2: Rodar o teste para ver falhar**

```bash
pytest tests/test_extractor.py -v
```
Expected: `ImportError` — módulo ainda não existe.

- [ ] **Step 3: Implementar extractor.py**

`src/extractor.py`:
```python
import os

class UnsupportedFormatError(Exception):
    pass

def extract_text(path: str) -> str:
    if not os.path.exists(path):
        raise FileNotFoundError(f"File not found: {path}")
    ext = os.path.splitext(path)[1].lower()
    if ext == ".md":
        return _extract_md(path)
    elif ext == ".pdf":
        return _extract_pdf(path)
    elif ext == ".docx":
        return _extract_docx(path)
    elif ext == ".xlsx":
        return _extract_xlsx(path)
    elif ext == ".bib":
        return _extract_bib(path)
    else:
        raise UnsupportedFormatError(f"Unsupported format: {ext}")

def _extract_md(path: str) -> str:
    with open(path, "r", encoding="utf-8", errors="ignore") as f:
        return f.read()

def _extract_pdf(path: str) -> str:
    import fitz  # pymupdf
    doc = fitz.open(path)
    return "\n".join(page.get_text() for page in doc)

def _extract_docx(path: str) -> str:
    from docx import Document
    doc = Document(path)
    return "\n".join(p.text for p in doc.paragraphs if p.text.strip())

def _extract_xlsx(path: str) -> str:
    import openpyxl
    wb = openpyxl.load_workbook(path, data_only=True)
    lines = []
    for sheet in wb.worksheets:
        lines.append(f"[Sheet: {sheet.title}]")
        for row in sheet.iter_rows(values_only=True):
            row_text = "\t".join(str(c) if c is not None else "" for c in row)
            if row_text.strip():
                lines.append(row_text)
    return "\n".join(lines)

def _extract_bib(path: str) -> str:
    import bibtexparser
    with open(path, "r", encoding="utf-8", errors="ignore") as f:
        library = bibtexparser.load(f)
    entries = []
    for entry in library.entries:
        parts = [f"[{entry.get('ENTRYTYPE', 'ref').upper()}] {entry.get('ID', '')}"]
        for field in ["title", "author", "year", "abstract", "journal", "booktitle"]:
            if field in entry:
                parts.append(f"{field}: {entry[field]}")
        entries.append("\n".join(parts))
    return "\n\n".join(entries)
```

- [ ] **Step 4: Rodar os testes**

```bash
pytest tests/test_extractor.py -v
```
Expected: 3 testes PASSED.

- [ ] **Step 5: Commit**

```bash
git add src/extractor.py tests/test_extractor.py
git commit -m "feat: text extractor for md, pdf, docx, xlsx, bib"
```

---

## Task 4: Chunker com metadados

**Files:**
- Create: `C:/workspace/pessoal/local-rag/src/chunker.py`
- Create: `C:/workspace/pessoal/local-rag/tests/test_chunker.py`

- [ ] **Step 1: Escrever os testes**

`tests/test_chunker.py`:
```python
from src.chunker import chunk_text

def test_short_text_single_chunk():
    chunks = chunk_text("Texto curto.", "fonte.md", chunk_size=2000, overlap=200)
    assert len(chunks) == 1
    assert chunks[0]["text"] == "Texto curto."
    assert chunks[0]["source"] == "fonte.md"
    assert chunks[0]["chunk_index"] == 0
    assert chunks[0]["total_chunks"] == 1

def test_long_text_multiple_chunks():
    text = "palavra " * 500  # ~4000 chars
    chunks = chunk_text(text, "longo.md", chunk_size=2000, overlap=200)
    assert len(chunks) >= 2
    for i, chunk in enumerate(chunks):
        assert chunk["chunk_index"] == i
        assert chunk["total_chunks"] == len(chunks)
        assert chunk["source"] == "longo.md"
        assert len(chunk["text"]) <= 2000 + 200  # chunk_size + overlap tolerance

def test_overlap_shares_content():
    text = "a" * 4000
    chunks = chunk_text(text, "overlap.md", chunk_size=2000, overlap=200)
    # O início do segundo chunk deve coincidir com o fim do primeiro
    end_of_first = chunks[0]["text"][-200:]
    start_of_second = chunks[1]["text"][:200]
    assert end_of_first == start_of_second

def test_metadata_preserved():
    chunks = chunk_text("texto", "D:/Obsidian/vault/tese.md", chunk_size=2000, overlap=200)
    assert chunks[0]["source"] == "D:/Obsidian/vault/tese.md"
```

- [ ] **Step 2: Rodar o teste para ver falhar**

```bash
pytest tests/test_chunker.py -v
```
Expected: `ImportError`.

- [ ] **Step 3: Implementar chunker.py**

`src/chunker.py`:
```python
from typing import List

def chunk_text(text: str, source: str, chunk_size: int = 2000, overlap: int = 200) -> List[dict]:
    if len(text) <= chunk_size:
        return [{"text": text, "source": source, "chunk_index": 0, "total_chunks": 1}]

    chunks = []
    start = 0
    while start < len(text):
        end = start + chunk_size
        chunk = text[start:end]
        chunks.append(chunk)
        start += chunk_size - overlap

    result = []
    total = len(chunks)
    for i, chunk in enumerate(chunks):
        result.append({
            "text": chunk,
            "source": source,
            "chunk_index": i,
            "total_chunks": total
        })
    return result
```

- [ ] **Step 4: Rodar os testes**

```bash
pytest tests/test_chunker.py -v
```
Expected: 4 testes PASSED.

- [ ] **Step 5: Commit**

```bash
git add src/chunker.py tests/test_chunker.py
git commit -m "feat: text chunker with overlap and metadata"
```

---

## Task 5: Checkpoint

**Files:**
- Create: `C:/workspace/pessoal/local-rag/src/checkpoint.py`
- Create: `C:/workspace/pessoal/local-rag/tests/test_checkpoint.py`

- [ ] **Step 1: Escrever os testes**

`tests/test_checkpoint.py`:
```python
import os
from src.checkpoint import Checkpoint

def test_new_file_not_processed(tmp_path):
    cp = Checkpoint(str(tmp_path / "checkpoint.json"))
    assert not cp.is_processed("/some/file.md", mtime=1000.0)

def test_mark_and_check(tmp_path):
    cp = Checkpoint(str(tmp_path / "checkpoint.json"))
    cp.mark_processed("/some/file.md", mtime=1000.0)
    assert cp.is_processed("/some/file.md", mtime=1000.0)

def test_modified_file_not_processed(tmp_path):
    cp = Checkpoint(str(tmp_path / "checkpoint.json"))
    cp.mark_processed("/some/file.md", mtime=1000.0)
    assert not cp.is_processed("/some/file.md", mtime=2000.0)

def test_persists_across_instances(tmp_path):
    path = str(tmp_path / "checkpoint.json")
    cp1 = Checkpoint(path)
    cp1.mark_processed("/file.md", mtime=1000.0)
    cp1.save()
    cp2 = Checkpoint(path)
    assert cp2.is_processed("/file.md", mtime=1000.0)
```

- [ ] **Step 2: Rodar o teste para ver falhar**

```bash
pytest tests/test_checkpoint.py -v
```
Expected: `ImportError`.

- [ ] **Step 3: Implementar checkpoint.py**

`src/checkpoint.py`:
```python
import json
import os

class Checkpoint:
    def __init__(self, path: str):
        self.path = path
        self._data: dict = {}
        if os.path.exists(path):
            with open(path, "r", encoding="utf-8") as f:
                self._data = json.load(f)

    def is_processed(self, file_path: str, mtime: float) -> bool:
        return self._data.get(file_path) == mtime

    def mark_processed(self, file_path: str, mtime: float) -> None:
        self._data[file_path] = mtime

    def save(self) -> None:
        os.makedirs(os.path.dirname(self.path), exist_ok=True)
        with open(self.path, "w", encoding="utf-8") as f:
            json.dump(self._data, f, indent=2)
```

- [ ] **Step 4: Rodar os testes**

```bash
pytest tests/test_checkpoint.py -v
```
Expected: 4 testes PASSED.

- [ ] **Step 5: Commit**

```bash
git add src/checkpoint.py tests/test_checkpoint.py
git commit -m "feat: checkpoint system to skip already-processed files"
```

---

## Task 6: Cliente HTTP do LightRAG

**Files:**
- Create: `C:/workspace/pessoal/local-rag/src/lightrag_client.py`
- Create: `C:/workspace/pessoal/local-rag/tests/test_lightrag_client.py`

- [ ] **Step 1: Escrever os testes**

`tests/test_lightrag_client.py`:
```python
import pytest
from unittest.mock import patch, MagicMock
from src.lightrag_client import LightRAGClient, LightRAGError

BASE_URL = "http://localhost:9621"

@pytest.fixture
def client():
    return LightRAGClient(host="localhost", port=9621)

def test_health_check_ok(client):
    with patch("requests.get") as mock_get:
        mock_get.return_value.status_code = 200
        mock_get.return_value.json.return_value = {"status": "ok"}
        assert client.health_check() is True

def test_health_check_fail(client):
    with patch("requests.get") as mock_get:
        mock_get.side_effect = Exception("connection refused")
        assert client.health_check() is False

def test_insert_document(client):
    with patch("requests.post") as mock_post:
        mock_post.return_value.status_code = 200
        mock_post.return_value.json.return_value = {"status": "ok"}
        client.insert_text("Texto de teste", description="test.md chunk 0/1")
        mock_post.assert_called_once()
        call_kwargs = mock_post.call_args
        assert "input_text" in call_kwargs.kwargs["json"] or "input_text" in call_kwargs[1]["json"]

def test_query(client):
    with patch("requests.post") as mock_post:
        mock_post.return_value.status_code = 200
        mock_post.return_value.json.return_value = {"response": "resultado da busca"}
        result = client.query("O que é performatividade?", mode="hybrid")
        assert result == "resultado da busca"

def test_query_raises_on_error(client):
    with patch("requests.post") as mock_post:
        mock_post.return_value.status_code = 500
        mock_post.return_value.text = "Internal Server Error"
        with pytest.raises(LightRAGError):
            client.query("pergunta", mode="hybrid")
```

- [ ] **Step 2: Rodar o teste para ver falhar**

```bash
pytest tests/test_lightrag_client.py -v
```
Expected: `ImportError`.

- [ ] **Step 3: Implementar lightrag_client.py**

`src/lightrag_client.py`:
```python
import requests

class LightRAGError(Exception):
    pass

class LightRAGClient:
    def __init__(self, host: str = "localhost", port: int = 9621):
        self.base_url = f"http://{host}:{port}"

    def health_check(self) -> bool:
        try:
            r = requests.get(f"{self.base_url}/health", timeout=5)
            return r.status_code == 200
        except Exception:
            return False

    def insert_text(self, text: str, description: str = "") -> None:
        r = requests.post(
            f"{self.base_url}/documents/text",
            json={"input_text": text, "description": description},
            timeout=120
        )
        if r.status_code != 200:
            raise LightRAGError(f"Insert failed [{r.status_code}]: {r.text}")

    def query(self, question: str, mode: str = "hybrid") -> str:
        r = requests.post(
            f"{self.base_url}/query",
            json={"query": question, "mode": mode},
            timeout=60
        )
        if r.status_code != 200:
            raise LightRAGError(f"Query failed [{r.status_code}]: {r.text}")
        return r.json()["response"]
```

- [ ] **Step 4: Rodar os testes**

```bash
pytest tests/test_lightrag_client.py -v
```
Expected: 5 testes PASSED.

- [ ] **Step 5: Commit**

```bash
git add src/lightrag_client.py tests/test_lightrag_client.py
git commit -m "feat: LightRAG HTTP client with insert and query"
```

---

## Task 7: Pipeline de ingestão

**Files:**
- Create: `C:/workspace/pessoal/local-rag/ingest.py`

- [ ] **Step 1: Iniciar o servidor LightRAG**

Abrir um terminal separado e executar:
```bash
lightrag-server --host 0.0.0.0 --port 9621 \
  --llm-provider ollama --llm-model qwen2.5:14b \
  --embedding-provider ollama --embedding-model nomic-embed-text \
  --working-dir C:/workspace/pessoal/local-rag/data/lightrag_storage
```
Aguardar a mensagem `Uvicorn running on http://0.0.0.0:9621`.

Verificar que o servidor responde:
```bash
curl http://localhost:9621/health
# Expected: {"status": "ok"}
```

- [ ] **Step 2: Implementar ingest.py**

`ingest.py`:
```python
import os
import sys
import argparse
from src.config_loader import load_config
from src.extractor import extract_text, UnsupportedFormatError
from src.chunker import chunk_text
from src.checkpoint import Checkpoint
from src.lightrag_client import LightRAGClient, LightRAGError

def discover_files(source_cfg: dict) -> list:
    path = source_cfg["path"]
    extensions = [e.lower() for e in source_cfg.get("extensions", [])]
    files = []
    for root, _, filenames in os.walk(path):
        for fname in filenames:
            ext = os.path.splitext(fname)[1].lower()
            if not extensions or ext in extensions:
                files.append(os.path.join(root, fname))
    return files

def run_ingest(config_path: str):
    config = load_config(config_path)
    client = LightRAGClient(
        host=config["lightrag"]["host"],
        port=config["lightrag"]["port"]
    )

    if not client.health_check():
        print("ERROR: LightRAG server not responding at "
              f"{config['lightrag']['host']}:{config['lightrag']['port']}")
        print("Start the server first with: lightrag-server ...")
        sys.exit(1)

    checkpoint = Checkpoint(config["checkpoint_file"])
    chunk_size = config["chunking"]["chunk_size"]
    overlap = config["chunking"]["overlap"]

    total_files = 0
    skipped = 0
    indexed = 0
    errors = 0

    for source_name, source_cfg in config["sources"].items():
        print(f"\n[{source_name}] Scanning {source_cfg['path']} ...")
        files = discover_files(source_cfg)
        print(f"  Found {len(files)} files")

        for file_path in files:
            mtime = os.path.getmtime(file_path)
            total_files += 1

            if checkpoint.is_processed(file_path, mtime):
                skipped += 1
                continue

            try:
                text = extract_text(file_path)
                if not text.strip():
                    checkpoint.mark_processed(file_path, mtime)
                    continue

                chunks = chunk_text(text, file_path, chunk_size, overlap)
                for chunk in chunks:
                    description = f"{chunk['source']} [{chunk['chunk_index']+1}/{chunk['total_chunks']}]"
                    client.insert_text(chunk["text"], description=description)

                checkpoint.mark_processed(file_path, mtime)
                checkpoint.save()
                indexed += 1
                print(f"  + {file_path} ({len(chunks)} chunks)")

            except UnsupportedFormatError:
                pass
            except LightRAGError as e:
                print(f"  ERROR indexing {file_path}: {e}")
                errors += 1
            except Exception as e:
                print(f"  ERROR processing {file_path}: {e}")
                errors += 1

    print(f"\nDone. {indexed} indexed, {skipped} skipped, {errors} errors (of {total_files} total files)")

if __name__ == "__main__":
    parser = argparse.ArgumentParser()
    parser.add_argument("--config", default="config.yaml")
    args = parser.parse_args()
    run_ingest(args.config)
```

- [ ] **Step 3: Rodar ingestão com Obsidian**

```bash
cd C:/workspace/pessoal/local-rag
python ingest.py --config config.yaml
```
Aguardar. A primeira execução pode demorar vários minutos dependendo do tamanho do vault.
Expected: linhas `+ D:/Obsidian/vault/... (N chunks)` para cada arquivo, seguido de resumo.

- [ ] **Step 4: Commit**

```bash
git add ingest.py
git commit -m "feat: ingestion pipeline with checkpoint and multi-source support"
```

---

## Task 8: MCP Server para Claude Code

**Files:**
- Create: `C:/workspace/pessoal/local-rag/mcp_server.py`
- Modify: `C:/Users/ricar/.claude/settings.json`

- [ ] **Step 1: Implementar mcp_server.py**

`mcp_server.py`:
```python
import asyncio
import sys
from mcp.server import Server
from mcp.server.stdio import stdio_server
from mcp import types
from src.config_loader import load_config
from src.lightrag_client import LightRAGClient, LightRAGError
from src.extractor import extract_text, UnsupportedFormatError
from src.chunker import chunk_text
from src.checkpoint import Checkpoint
import os

CONFIG_PATH = os.path.join(os.path.dirname(__file__), "config.yaml")
config = load_config(CONFIG_PATH)
client = LightRAGClient(
    host=config["lightrag"]["host"],
    port=config["lightrag"]["port"]
)

server = Server("lightrag-local")

@server.list_tools()
async def list_tools() -> list[types.Tool]:
    return [
        types.Tool(
            name="query_rag",
            description="Consulta a base de conhecimento RAG local (Obsidian + Zotero)",
            inputSchema={
                "type": "object",
                "properties": {
                    "question": {"type": "string", "description": "Pergunta ou tema a pesquisar"},
                    "mode": {
                        "type": "string",
                        "enum": ["local", "global", "hybrid"],
                        "default": "hybrid",
                        "description": "local=trechos específicos, global=visão ampla, hybrid=ambos"
                    }
                },
                "required": ["question"]
            }
        ),
        types.Tool(
            name="insert_document",
            description="Indexa um arquivo específico no RAG",
            inputSchema={
                "type": "object",
                "properties": {
                    "path": {"type": "string", "description": "Caminho absoluto do arquivo"}
                },
                "required": ["path"]
            }
        ),
        types.Tool(
            name="rag_health",
            description="Verifica se o servidor LightRAG está online",
            inputSchema={"type": "object", "properties": {}}
        )
    ]

@server.call_tool()
async def call_tool(name: str, arguments: dict) -> list[types.TextContent]:
    if name == "query_rag":
        mode = arguments.get("mode", "hybrid")
        try:
            result = client.query(arguments["question"], mode=mode)
            return [types.TextContent(type="text", text=result)]
        except LightRAGError as e:
            return [types.TextContent(type="text", text=f"Erro na consulta: {e}")]

    elif name == "insert_document":
        path = arguments["path"]
        try:
            text = extract_text(path)
            cfg = config["chunking"]
            chunks = chunk_text(text, path, cfg["chunk_size"], cfg["overlap"])
            for chunk in chunks:
                desc = f"{chunk['source']} [{chunk['chunk_index']+1}/{chunk['total_chunks']}]"
                client.insert_text(chunk["text"], description=desc)
            return [types.TextContent(type="text", text=f"Indexado: {path} ({len(chunks)} chunks)")]
        except UnsupportedFormatError as e:
            return [types.TextContent(type="text", text=f"Formato não suportado: {e}")]
        except LightRAGError as e:
            return [types.TextContent(type="text", text=f"Erro ao indexar: {e}")]

    elif name == "rag_health":
        ok = client.health_check()
        status = "online" if ok else "offline"
        return [types.TextContent(type="text", text=f"LightRAG server: {status}")]

    return [types.TextContent(type="text", text=f"Unknown tool: {name}")]

async def main():
    async with stdio_server() as (read_stream, write_stream):
        await server.run(read_stream, write_stream, server.create_initialization_options())

if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 2: Testar o MCP server manualmente**

```bash
cd C:/workspace/pessoal/local-rag
echo '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}' | python mcp_server.py
```
Expected: resposta JSON com as 3 ferramentas listadas.

- [ ] **Step 3: Registrar no settings.json do Claude Code**

Abrir `C:/Users/ricar/.claude/settings.json` e adicionar dentro de `"mcpServers"`:
```json
"lightrag": {
  "command": "python",
  "args": ["C:/workspace/pessoal/local-rag/mcp_server.py"],
  "cwd": "C:/workspace/pessoal/local-rag"
}
```

- [ ] **Step 4: Reiniciar Claude Code e verificar**

Após reiniciar, no terminal do Claude Code executar:
```
use the rag_health tool
```
Expected: `LightRAG server: online`

- [ ] **Step 5: Commit**

```bash
git add mcp_server.py
git commit -m "feat: MCP server with query_rag, insert_document, rag_health tools"
```

---

## Task 9: Watcher daemon

**Files:**
- Create: `C:/workspace/pessoal/local-rag/watcher.py`

- [ ] **Step 1: Implementar watcher.py**

`watcher.py`:
```python
import time
import os
import sys
from src.config_loader import load_config
from src.extractor import extract_text, UnsupportedFormatError
from src.chunker import chunk_text
from src.checkpoint import Checkpoint
from src.lightrag_client import LightRAGClient, LightRAGError

def get_all_files(config: dict) -> list:
    files = []
    for source_cfg in config["sources"].values():
        path = source_cfg["path"]
        extensions = [e.lower() for e in source_cfg.get("extensions", [])]
        for root, _, filenames in os.walk(path):
            for fname in filenames:
                ext = os.path.splitext(fname)[1].lower()
                if not extensions or ext in extensions:
                    files.append(os.path.join(root, fname))
    return files

def run_watcher(config_path: str, interval: int = 60):
    config = load_config(config_path)
    client = LightRAGClient(
        host=config["lightrag"]["host"],
        port=config["lightrag"]["port"]
    )
    checkpoint = Checkpoint(config["checkpoint_file"])
    chunk_size = config["chunking"]["chunk_size"]
    overlap = config["chunking"]["overlap"]

    print(f"Watcher iniciado. Verificando a cada {interval}s. Ctrl+C para parar.")
    while True:
        for file_path in get_all_files(config):
            try:
                mtime = os.path.getmtime(file_path)
            except FileNotFoundError:
                continue

            if checkpoint.is_processed(file_path, mtime):
                continue

            try:
                text = extract_text(file_path)
                if not text.strip():
                    checkpoint.mark_processed(file_path, mtime)
                    continue
                chunks = chunk_text(text, file_path, chunk_size, overlap)
                for chunk in chunks:
                    desc = f"{chunk['source']} [{chunk['chunk_index']+1}/{chunk['total_chunks']}]"
                    client.insert_text(chunk["text"], description=desc)
                checkpoint.mark_processed(file_path, mtime)
                checkpoint.save()
                print(f"[watcher] Indexed: {file_path}")
            except (UnsupportedFormatError, LightRAGError, Exception):
                pass

        time.sleep(interval)

if __name__ == "__main__":
    import argparse
    parser = argparse.ArgumentParser()
    parser.add_argument("--config", default="config.yaml")
    parser.add_argument("--interval", type=int, default=60)
    args = parser.parse_args()
    run_watcher(args.config, args.interval)
```

- [ ] **Step 2: Testar o watcher**

```bash
cd C:/workspace/pessoal/local-rag
python watcher.py --config config.yaml --interval 30
```
Criar uma nota nova no Obsidian e aguardar até 30s.
Expected: `[watcher] Indexed: D:/Obsidian/vault/nova_nota.md`

- [ ] **Step 3: Commit final**

```bash
git add watcher.py
git commit -m "feat: watcher daemon for auto-indexing new files"
```

---

## Task 10: Configuração do Obsidian (Smart Connections)

> Esta task é configuração manual — sem código.

- [ ] **Step 1: Instalar plugin Smart Connections no Obsidian**

1. Abrir Obsidian → Settings → Community plugins → Browse
2. Buscar "Smart Connections"
3. Instalar e ativar

- [ ] **Step 2: Configurar endpoint local**

Em Settings → Smart Connections:
- **Model**: Local
- **API Base URL**: `http://localhost:9621`
- Salvar configuração

- [ ] **Step 3: Testar busca semântica no Obsidian**

Abrir qualquer nota no vault e usar o painel Smart Connections (ícone na barra lateral).
Expected: notas relacionadas ao conteúdo da nota atual aparecem listadas.

---

## Verificação Final

Após concluir todas as tasks, verificar o sistema completo:

```bash
# 1. Servidor rodando
curl http://localhost:9621/health

# 2. Ingestão completa
python ingest.py --config config.yaml

# 3. Query de teste via Python direto
python -c "
from src.lightrag_client import LightRAGClient
c = LightRAGClient()
print(c.query('O que é performatividade criptoeconômica?', mode='hybrid'))
"

# 4. Todos os testes passando
pytest tests/ -v
```

---

## Referências

- [HKUDS/LightRAG](https://github.com/HKUDS/LightRAG)
- [LightRAG API README](https://github.com/HKUDS/LightRAG/blob/main/lightrag/api/README.md)
- [MCP Python SDK](https://github.com/modelcontextprotocol/python-sdk)
- [Ollama](https://ollama.com)
